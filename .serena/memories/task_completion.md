Before handing off a task: (1) run `dotnet build ProductCatalog.sln` and the relevant `dotnet test` targets (plus `-p:CollectCoverage=true` when editing commands/handlers/store actions); (2) execute `scripts/validate-vsa-structure.sh` if you touched slice layout or added projects; (3) apply required database migrations with `dotnet ef database update --project src/ProductCatalog/Shared/Infrastructure/Persistence/... --startup-project src/ProductCatalog.Host/...` whenever persistence changed; (4) document any runtime or migration changes in `README.md` or `README_DATABASE.md`; (5) capture UI evidence (screenshots/Playwright artifacts) for PRs that alter visible behavior.