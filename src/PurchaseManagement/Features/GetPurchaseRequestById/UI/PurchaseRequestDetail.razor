@page "/purchase-requests/{Id:guid}"
@using MediatR
@using GetPurchaseRequestById.Application
@using ApprovePurchaseRequest.Application
@using RejectPurchaseRequest.Application
@using Shared.Application
@using Microsoft.AspNetCore.Components.Authorization
@using System.Security.Claims
@inject IMediator Mediator
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthenticationStateProvider

<PageTitle>購買申請詳細</PageTitle>

@if (_isLoading)
{
    <div class="text-center">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">読み込み中...</span>
        </div>
    </div>
}
else if (!string.IsNullOrEmpty(_errorMessage))
{
    <div class="alert alert-danger" role="alert">
        @_errorMessage
    </div>
    <button class="btn btn-secondary" @onclick="@(() => Navigation.NavigateTo("/purchase-requests"))">
        一覧に戻る
    </button>
}
else if (_purchaseRequest == null)
{
    <div class="alert alert-warning" role="alert">
        指定された購買申請が見つかりませんでした。
    </div>
    <button class="btn btn-secondary" @onclick="@(() => Navigation.NavigateTo("/purchase-requests"))">
        一覧に戻る
    </button>
}
else
{
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>購買申請詳細</h3>
        <button class="btn btn-secondary" @onclick="@(() => Navigation.NavigateTo("/purchase-requests"))">
            一覧に戻る
        </button>
    </div>

    @if (!string.IsNullOrEmpty(_actionMessage))
    {
        <div class="alert @(_actionSuccess ? "alert-success" : "alert-danger")" role="alert">
            @_actionMessage
        </div>
    }

    <div class="card mb-4">
        <div class="card-header">
            <h5>基本情報</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <strong>申請番号:</strong> @_purchaseRequest.RequestNumber
                </div>
                <div class="col-md-6 mb-3">
                    <strong>ステータス:</strong>
                    <span class="badge @GetStatusBadgeClass(_purchaseRequest.Status)">
                        @_purchaseRequest.StatusName
                    </span>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <strong>申請者:</strong> @_purchaseRequest.RequesterName
                </div>
                <div class="col-md-6 mb-3">
                    <strong>作成日時:</strong> @_purchaseRequest.CreatedAt.ToLocalTime().ToString("yyyy/MM/dd HH:mm")
                </div>
            </div>
            <div class="row">
                <div class="col-12 mb-3">
                    <strong>タイトル:</strong> @_purchaseRequest.Title
                </div>
            </div>
            <div class="row">
                <div class="col-12 mb-3">
                    <strong>説明:</strong>
                    <p class="mt-2">@_purchaseRequest.Description</p>
                </div>
            </div>
            @if (_purchaseRequest.SubmittedAt.HasValue)
            {
                <div class="row">
                    <div class="col-md-4 mb-3">
                        <strong>提出日時:</strong> @_purchaseRequest.SubmittedAt.Value.ToLocalTime().ToString("yyyy/MM/dd HH:mm")
                    </div>
                </div>
            }
            @if (_purchaseRequest.ApprovedAt.HasValue)
            {
                <div class="row">
                    <div class="col-md-4 mb-3">
                        <strong>承認日時:</strong> @_purchaseRequest.ApprovedAt.Value.ToLocalTime().ToString("yyyy/MM/dd HH:mm")
                    </div>
                </div>
            }
            @if (_purchaseRequest.RejectedAt.HasValue)
            {
                <div class="row">
                    <div class="col-md-4 mb-3">
                        <strong>却下日時:</strong> @_purchaseRequest.RejectedAt.Value.ToLocalTime().ToString("yyyy/MM/dd HH:mm")
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            <h5>購入品目</h5>
        </div>
        <div class="card-body">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>商品名</th>
                        <th>単価</th>
                        <th>数量</th>
                        <th>金額</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in _purchaseRequest.Items)
                    {
                        <tr>
                            <td>@item.ProductName</td>
                            <td>@item.UnitPrice.ToString("N2") @item.Currency</td>
                            <td>@item.Quantity</td>
                            <td>@item.Amount.ToString("N2") @item.Currency</td>
                        </tr>
                    }
                </tbody>
                <tfoot>
                    <tr>
                        <th colspan="3" class="text-end">合計:</th>
                        <th>@_purchaseRequest.TotalAmount.ToString("N2") @_purchaseRequest.Currency</th>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>

    @if (_purchaseRequest.ApprovalSteps.Any())
    {
        <div class="card mb-4">
            <div class="card-header">
                <h5>承認フロー</h5>
            </div>
            <div class="card-body">
                @foreach (var step in _purchaseRequest.ApprovalSteps.OrderBy(s => s.StepNumber))
                {
                    <div class="card mb-3 @(step.Status == 0 ? "border-warning" : step.Status == 1 ? "border-success" : "border-danger")">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <strong>第@(step.StepNumber)承認者:</strong> @step.ApproverName (@step.ApproverRole)
                                </div>
                                <div class="col-md-6">
                                    <strong>ステータス:</strong>
                                    <span class="badge @GetApprovalStepStatusBadgeClass(step.Status)">
                                        @step.StatusName
                                    </span>
                                </div>
                            </div>
                            @if (!string.IsNullOrEmpty(step.Comment))
                            {
                                <div class="row mt-2">
                                    <div class="col-12">
                                        <strong>コメント:</strong> @step.Comment
                                    </div>
                                </div>
                            }
                            @if (step.ApprovedAt.HasValue)
                            {
                                <div class="row mt-2">
                                    <div class="col-12">
                                        <small class="text-muted">承認日時: @step.ApprovedAt.Value.ToLocalTime().ToString("yyyy/MM/dd HH:mm")</small>
                                    </div>
                                </div>
                            }
                            @if (step.RejectedAt.HasValue)
                            {
                                <div class="row mt-2">
                                    <div class="col-12">
                                        <small class="text-muted">却下日時: @step.RejectedAt.Value.ToLocalTime().ToString("yyyy/MM/dd HH:mm")</small>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
        </div>
    }

    @if (CanApprove() || CanReject())
    {
        <div class="card mb-4">
            <div class="card-header">
                <h5>アクション</h5>
            </div>
            <div class="card-body">
                @if (CanApprove())
                {
                    <div class="mb-3">
                        <label for="approvalComment" class="form-label">承認コメント</label>
                        <textarea id="approvalComment" class="form-control" @bind="_approvalComment" rows="3"></textarea>
                    </div>
                    <button class="btn btn-success" @onclick="HandleApprove" disabled="@_isProcessing">
                        @if (_isProcessing)
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                        }
                        承認
                    </button>
                }
                @if (CanReject())
                {
                    <div class="mb-3">
                        <label for="rejectionReason" class="form-label">却下理由 (必須)</label>
                        <textarea id="rejectionReason" class="form-control" @bind="_rejectionReason" rows="3"></textarea>
                    </div>
                    <button class="btn btn-danger" @onclick="HandleReject" disabled="@(_isProcessing || string.IsNullOrWhiteSpace(_rejectionReason))">
                        @if (_isProcessing)
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                        }
                        却下
                    </button>
                }
            </div>
        </div>
    }
}

@code {
    [Parameter]
    public Guid Id { get; set; }

    private PurchaseRequestDetailDto? _purchaseRequest;
    private bool _isLoading = true;
    private string? _errorMessage;
    private string? _actionMessage;
    private bool _actionSuccess;
    private bool _isProcessing;

    private string _approvalComment = string.Empty;
    private string _rejectionReason = string.Empty;

    // SECURITY: Current user ID for authorization checks
    private Guid _currentUserId = Guid.Empty;

    protected override async Task OnInitializedAsync()
    {
        // Get current user ID from authentication state
        var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;

        if (user.Identity?.IsAuthenticated == true)
        {
            var userIdClaim = user.FindFirst(ClaimTypes.NameIdentifier)?.Value;
            _currentUserId = Guid.TryParse(userIdClaim, out var userId) ? userId : Guid.Empty;
        }
    }

    protected override async Task OnParametersSetAsync()
    {
        await LoadPurchaseRequest();
    }

    private async Task LoadPurchaseRequest()
    {
        _isLoading = true;
        _errorMessage = null;

        try
        {
            var query = new GetPurchaseRequestByIdQuery { Id = Id };
            var result = await Mediator.Send(query);

            if (result.IsSuccess)
            {
                _purchaseRequest = result.Value;
            }
            else
            {
                _errorMessage = result.Error;
            }
        }
        catch (Exception ex)
        {
            _errorMessage = $"予期しないエラーが発生しました: {ex.Message}";
        }
        finally
        {
            _isLoading = false;
        }
    }

    private async Task HandleApprove()
    {
        _isProcessing = true;
        _actionMessage = null;

        try
        {
            var command = new ApprovePurchaseRequestCommand
            {
                RequestId = Id,
                Comment = _approvalComment
            };

            var result = await Mediator.Send(command);

            if (result.IsSuccess)
            {
                _actionMessage = "購買申請を承認しました。";
                _actionSuccess = true;
                _approvalComment = string.Empty;
                await LoadPurchaseRequest();
            }
            else
            {
                _actionMessage = $"承認エラー: {result.Error}";
                _actionSuccess = false;
            }
        }
        catch (Exception ex)
        {
            _actionMessage = $"予期しないエラーが発生しました: {ex.Message}";
            _actionSuccess = false;
        }
        finally
        {
            _isProcessing = false;
        }
    }

    private async Task HandleReject()
    {
        _isProcessing = true;
        _actionMessage = null;

        try
        {
            var command = new RejectPurchaseRequestCommand
            {
                RequestId = Id,
                Reason = _rejectionReason
            };

            var result = await Mediator.Send(command);

            if (result.IsSuccess)
            {
                _actionMessage = "購買申請を却下しました。";
                _actionSuccess = true;
                _rejectionReason = string.Empty;
                await LoadPurchaseRequest();
            }
            else
            {
                _actionMessage = $"却下エラー: {result.Error}";
                _actionSuccess = false;
            }
        }
        catch (Exception ex)
        {
            _actionMessage = $"予期しないエラーが発生しました: {ex.Message}";
            _actionSuccess = false;
        }
        finally
        {
            _isProcessing = false;
        }
    }

    /// <summary>
    /// 承認可否の判定
    ///
    /// SECURITY: 現在のユーザーが承認者として割り当てられているかをチェック
    /// - 単なるステータスチェックでは不十分（誰でも承認できてしまう）
    /// - CurrentApprovalStep（Status = 0, Pending）の承認者IDと現在のユーザーIDを突き合わせる
    /// </summary>
    private bool CanApprove()
    {
        if (_purchaseRequest == null || _currentUserId == Guid.Empty) return false;

        // Find the current pending approval step (Status = 0)
        var currentStep = _purchaseRequest.ApprovalSteps
            .FirstOrDefault(s => s.Status == 0); // 0 = Pending

        if (currentStep == null) return false;

        // CRITICAL: Check if current user is the approver for this step
        return currentStep.ApproverId == _currentUserId;
    }

    /// <summary>
    /// 却下可否の判定
    ///
    /// SECURITY: 現在のユーザーが承認者として割り当てられているかをチェック
    /// - 承認と同じロジック（現在のステップの承認者のみが却下可能）
    /// </summary>
    private bool CanReject()
    {
        if (_purchaseRequest == null || _currentUserId == Guid.Empty) return false;

        // Find the current pending approval step (Status = 0)
        var currentStep = _purchaseRequest.ApprovalSteps
            .FirstOrDefault(s => s.Status == 0); // 0 = Pending

        if (currentStep == null) return false;

        // CRITICAL: Check if current user is the approver for this step
        return currentStep.ApproverId == _currentUserId;
    }

    private static string GetStatusBadgeClass(int status)
    {
        return status switch
        {
            0 => "bg-secondary", // 下書き
            1 => "bg-info", // 提出済み
            2 or 3 or 4 => "bg-warning", // 承認待ち
            5 => "bg-success", // 承認済み
            6 => "bg-danger", // 却下
            7 => "bg-dark", // キャンセル
            _ => "bg-light"
        };
    }

    private static string GetApprovalStepStatusBadgeClass(int status)
    {
        return status switch
        {
            0 => "bg-warning", // 保留中
            1 => "bg-success", // 承認済み
            2 => "bg-danger", // 却下
            _ => "bg-light"
        };
    }
}
